
#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int main(){
    ll x;
    cin >> x;
    ll m=1;
    while(x!=1){
        if(x%2==1){
            x=x-1;
            x=(x/2);
            m++;
        }
        else{
            x=(x/2);
        }
    }
    cout << m << "\n";
}
