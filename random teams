#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
#define f(i,a,b) for(ll i=a;i<b;i++)
int main(){
    ll n,m;
    cin >> n >> m;
    ll x=(n/m);
    ll y=n-(n/m)*m;
    cout << ((y*(x+1)*(x))/2) + ((m-y)*x*(x-1))/2 << " ";
    cout << ((n+1-m)*(n-m))/2 << "\n";
}
